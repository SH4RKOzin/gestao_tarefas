
para primeia vez
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/SH4RKOzin/gestao_tarefas.git
git push -u origin main 


git add .
git status
git commit -a -m "ultimo commit"
git push origin main



<script>
    // Função para mostrar a imagem de perfil selecionada
    function previewImage(event) {
        var input = event.target;
        var reader = new FileReader();

        reader.onload = function() {
            var dataURL = reader.result;
            document.getElementById('photo').src = dataURL; // Atualiza a imagem de perfil exibida
        };

        reader.readAsDataURL(input.files[0]); // Lê o arquivo selecionado
    }
</script>
 .user-img {
            width: 130px;
            height: 130px;
            border-radius: 50%;
            margin: 15% 45%; /* Ajustado para melhor posicionamento */
            position: relative; /* Adicionado para posicionar o botão de upload */
        }

        #photo {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            object-fit: cover; /* Ajusta o redimensionamento da imagem */
        }

        #file {
            display: none;
        }

        #uploadbtn {
            position: absolute;
            height: 30px;
            width: 30px;
            padding: 6px 6px;
            border-radius: 50%;
            cursor: pointer;
            color: #fff;
            background-color: #007bff; /* Cor alterada para azul */
            bottom: 0; /* Posicionamento ajustado */
            right: 0; /* Posicionamento ajustado */
            display: flex;
            justify-content: center;
            align-items: center;
        }
        <div class="user-img">
                        <img src="perfil.png" id="photo">
                        <input type="file" id="file" name="imagem" accept="image/*" onchange="previewImage(event)">
                        <label for="file" id="uploadbtn"><i class="fas fa-camera"></i></label>
                    </div>